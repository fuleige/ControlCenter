# 仅供旧节点迁移期间使用；新节点使用 Web 生成的一次性注册 Token。
AGENT_SHARED_TOKEN=replace-with-a-long-random-legacy-value
# 可选，仅用于首次启动引导；之后以 /data/secrets/admin-token 文件为准。
ADMIN_TOKEN=
WEB_ORIGIN=https://control.example.com
# 默认留空，浏览器通过 Web 容器的同源反向代理访问控制中心。
PUBLIC_CONTROL_API_URL=
WEB_PORT=8080
